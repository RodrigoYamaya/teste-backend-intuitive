spring.application.name=analise-dados-an

# Conex�o com Banco
spring.datasource.url=jdbc:mysql://localhost:3306/intuitive_care_ans?useTimezone=true&serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Atualiza��o de Schema
spring.jpa.hibernate.ddl-auto=update

# --- [AQUI EST� A MUDAN�A PARA FICAR R�PIDO] ---
# Coloque FALSE para n�o imprimir cada insert na tela
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# Garante sil�ncio total do Log do Hibernate (Modo Ninja ?)
logging.level.org.hibernate.SQL=off
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=off

# Configura��es de Performance (J� estavam �timas, mantive)
spring.jpa.properties.hibernate.jdbc.batch_size=1000
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true